spring:
  application:
    name: gateway
  profiles:
    active: dev
  config:
    import: optional:configserver:http://admin:1234@localhost:9000

management:
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: gateway
